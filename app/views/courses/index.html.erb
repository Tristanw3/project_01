<div class="container">
  <% @courses.each do |c| %>
    <div class="card">
      <h3><%= c.course_name %></h3>
      <img src=<%=c.image%> alt="">

      <%= c.user.name%>
    </div>
  
  <% end %>
</div>
